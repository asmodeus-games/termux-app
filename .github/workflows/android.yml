name: Android Build

on:
  push:
    branches: [main, master, ssh-integration]
    tags: ['v*']
  pull_request:
    branches: [main, master, ssh-integration]

permissions:
  contents: write

env:
  ANDROID_SDK_VERSION: 36
  GRADLE_VERSION: 8.13
  TERMUX_PACKAGE_VARIANT: apt-android-7
  TERMUX_SPLIT_APKS_FOR_RELEASE_BUILDS: "1"

jobs:
  build:
    name: Build Android APK
    runs-on: ubuntu-latest
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        package_variant: [ apt-android-7, apt-android-5 ]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: gradle

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install additional SDK packages
        run: |
          yes | sdkmanager --licenses
          sdkmanager "platforms;android-36" "build-tools;35.0.0" "ndk;26.1.10909125" "cmake;3.22.1" "platform-tools"

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Download Termux bootstrap packages
        run: ./gradlew downloadBootstraps --no-daemon -q
        env:
          TERMUX_PACKAGE_VARIANT: ${{ matrix.package_variant }}

      - name: Build Debug APK
        run: ./gradlew :app:assembleDebug --no-daemon -q
        env:
          TERMUX_PACKAGE_VARIANT: ${{ matrix.package_variant }}

      - name: Build Release APK (on tags only)
        if: startsWith(github.ref, 'refs/tags/')
        run: ./gradlew :app:assembleRelease --no-daemon -q
        env:
          TERMUX_PACKAGE_VARIANT: ${{ matrix.package_variant }}
          TERMUX_SPLIT_APKS_FOR_RELEASE_BUILDS: "1"

      - name: Upload Debug APKs as Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: termux-debug-apks-${{ matrix.package_variant }}
          path: app/build/outputs/apk/debug/*.apk
          retention-days: 30

      - name: Upload Release APKs as Artifacts
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-artifact@v4
        with:
          name: termux-release-apks-${{ matrix.package_variant }}
          path: app/build/outputs/apk/release/*.apk
          retention-days: 30

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/') && matrix.package_variant == 'apt-android-7'
        uses: softprops/action-gh-release@v2
        with:
          files: app/build/outputs/apk/release/*.apk
          generate_release_notes: true
          draft: false
          prerelease: false

      - name: Deploy index.html to GitHub Pages
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .
          publish_branch: gh-pages
          keep_files: true
          force_orphan: true
          include_assets: "index.html"
          cname: ""

  lint:
    name: Lint & Check
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: gradle

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install additional SDK packages
        run: |
          yes | sdkmanager --licenses
          sdkmanager "platforms;android-36" "build-tools;35.0.0"

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run Lint
        run: ./gradlew :app:lintDebug --no-daemon -q

      - name: Run Unit Tests
        run: ./gradlew :app:testDebugUnitTest :ssh-integration:test --no-daemon -q